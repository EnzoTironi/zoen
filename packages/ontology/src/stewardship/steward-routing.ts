// @zoen-plan packages/ontology/src/stewardship/steward-routing.ts
// NON-EXECUTABLE PSEUDOCODE; not registered or compiled as product implementation.
// # File plan — `packages/ontology/src/stewardship/steward-routing.ts`
//
// **Status:** planned; no product acceptance implied.
//
// Target: `packages/ontology/src/stewardship/steward-routing.ts`. Representation: **comment-only-source**. Allocation: **required**.
//
// Specs: [SPEC-017](../../../../docs/specs/spec-017.md).
// Tickets: [ZN-0102](../../../../docs/tickets/zn-0102.md).
//
// ## Responsibility and reuse
//
// ```text
// PROCEDURE ZN_0102 /* planning label, not a public API */
//   OWNER := SPEC-017; TARGET := packages/ontology/src/stewardship/steward-routing.ts
//   REQUIRE accepted dependencies: ZN-0101
//   REQUIRE evidence layer: component; actual admitted services when needed
//   IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
//   IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
//   USE the shared module protocol below; implement ONLY this ticket's segment, not a duplicate engine.
//     IDENTIFY unresolved decisions at an authorized basis; compute consequence, deadline and reducibility from declared weights.
//     DEDUPLICATE question by World/subject/predicate/scope/candidate version; suppress stale or inaccessible candidates.
//     ROUTE only to an authorized steward who may see the evidence needed for the question.
//     PIN question digest, candidates and intended answer kind before presentation.
//     RECHECK authority, candidate basis and question freshness when response arrives.
//     DISTINGUISH assertion, identity choice, local decision, reusable-rule proposal and preference; dismissal/unknown is not a vote.
//     APPLY local correction via ordinary operation; reusable changes enter evaluation/publication separately.
//     TRACK unanswered/stopped states and interruption costs without promoting uncertainty to fabricated truth.
//   TICKET-SPECIFIC SEGMENT:
//     01. Require permission to inspect required evidence and current stewardship/mandate scope.
//     02. Do not infer competence from job title, group presence or agent suggestion.
//     03. Route unresolved assignment to the exception inbox without broad disclosure.
//   TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
//     GIVEN A receptionist can read schedule but not clinical notes required by a question
//     WHEN The router considers that user
//     THEN It does not disclose the question or evidence; an authorized steward or explicit blocked state is selected
//   ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
//   RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
// ```
//
// ## Acceptance boundary
//
// A plan is not implementation, and a compile of comment-only files proves no behavior. All relevant ticket check IDs must execute at their required layer with independent evidence. Services are not mocked; missing credentials/dependencies remain blockers.
