/**
 * Ontology package entry — authority/surface composition root.
 * Clients and Eve must import released ports/clients, not raw adapters.
 */
export { SemanticExecutor } from './surfaces/dispatch.js';
export { Authority } from './authority/transaction.js';
