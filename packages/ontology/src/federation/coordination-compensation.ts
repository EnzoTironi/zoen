// @zoen-plan packages/ontology/src/federation/coordination-compensation.ts
// NON-EXECUTABLE PSEUDOCODE; not registered or compiled as product implementation.
// # File plan — `packages/ontology/src/federation/coordination-compensation.ts`
//
// **Status:** planned; no product acceptance implied.
//
// Target: `packages/ontology/src/federation/coordination-compensation.ts`. Representation: **comment-only-source**. Allocation: **required**.
//
// Specs: [SPEC-043](../../../../docs/specs/spec-043.md).
// Tickets: [ZN-0253](../../../../docs/tickets/zn-0253.md).
//
// ## Responsibility and reuse
//
// ```text
// PROCEDURE ZN_0253 /* planning label, not a public API */
//   OWNER := SPEC-043; TARGET := packages/ontology/src/federation/coordination-compensation.ts
//   REQUIRE accepted dependencies: ZN-0252
//   REQUIRE evidence layer: component; actual admitted services when needed
//   IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
//   IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
//   USE the shared module protocol below; implement ONLY this ticket's segment, not a duplicate engine.
//     VERIFY remote service identity and principal-bound delegated purpose independently at each World.
//     REQUEST each component Frame through its normal semantic surface and current source/license restrictions.
//     BUILD federated Frame retaining separate cuts, time skew, gaps and inherited rights; no global grant.
//     PROPOSE coordination as explicit participant plans with independent local approvals and guards.
//     COMMIT each participant locally; record accepted/rejected/unknown separately and derive honest partial global outcome.
//     CONSERVE global budget/inventory through designated ownership or pre-partitioned reservations, not messaging assumptions.
//     RETRY/reconcile stable participant identities; compensation is a new local ActionCase.
//     ON revocation or participant loss stop unauthorized future disclosure/work while preserving already observed receipts.
//   TICKET-SPECIFIC SEGMENT:
//     01. Model compensation as new authorized local Actions.
//     02. For shared budgets identify one owning World or explicit partitioned allocation contract.
//     03. Block operations that assume unavailable cross-World atomicity.
//   TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
//     GIVEN Two companies try to spend the same unpartitioned global reserve independently
//     WHEN The coordination planner validates them
//     THEN It rejects the unsafe plan or routes reservation to the single owner; messages alone do not create atomic funds
//   ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
//   RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
// ```
//
// ## Acceptance boundary
//
// A plan is not implementation, and a compile of comment-only files proves no behavior. All relevant ticket check IDs must execute at their required layer with independent evidence. Services are not mocked; missing credentials/dependencies remain blockers.
