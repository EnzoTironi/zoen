// @zoen-plan packages/ontology/src/apps/app-definition.ts
// NON-EXECUTABLE PSEUDOCODE; not registered or compiled as product implementation.
// # File plan — `packages/ontology/src/apps/app-definition.ts`
//
// **Status:** planned; no product acceptance implied.
//
// Target: `packages/ontology/src/apps/app-definition.ts`. Representation: **comment-only-source**. Allocation: **required**.
//
// Specs: [SPEC-052](../../../../docs/specs/spec-052.md).
// Tickets: [ZN-0302](../../../../docs/tickets/zn-0302.md).
//
// ## Responsibility and reuse
//
// ```text
// PROCEDURE ZN_0302 /* planning label, not a public API */
//   OWNER := SPEC-052; TARGET := packages/ontology/src/apps/app-definition.ts
//   REQUIRE accepted dependencies: ZN-0078, ZN-0291
//   REQUIRE evidence layer: component; actual admitted services when needed
//   IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
//   IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
//   USE the shared module protocol below; implement ONLY this ticket's segment, not a duplicate engine.
//     VALIDATE immutable app definition against approved component versions, typed bindings, resource limits and accessibility labels.
//     REJECT arbitrary script/HTML/eval/provider URLs/SQL and hidden data bindings in declarative mode.
//     RESOLVE query/action bindings to stable released semantic operations; presentation cannot manufacture authoritative metrics.
//     INSTANTIATE template parameters as ordinary allowed instance change only when semantics/powers stay unchanged.
//     FOR changed definitions use existing compile/evaluate/prepare/approve/activate path; create, publish and share are separate.
//     OPEN app through protected session and reuse the common semantic client for all data, subscriptions and exports.
//     RENDER read-only defaults, conflicts and missing values faithfully; consequential forms use trusted ActionCase confirmation.
//     UPGRADE/uninstall/recall through versioned release/artifact lifecycle; no mutable runtime latest alias as authority.
//   TICKET-SPECIFIC SEGMENT:
//     01. Add declarative routes, components, form bindings, read/action references and declared capability ceiling to the released grammar.
//     02. Validate stable IDs, dependency closure, labels, bounded nodes and per-view query budgets.
//     03. Refuse executable expressions and external fetch URLs; use a template instance lane only when the existing policy explicitly covers exact parameters.
//   TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
//     GIVEN An admitted order type, production view and existing Inspect operation
//     WHEN A model proposes a board with table/detail/filter widgets and released bindings
//     THEN A deterministic validated manifest is emitted without adding TypeScript domain code or a new backend
//   ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
//   RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
// ```
//
// ## Acceptance boundary
//
// A plan is not implementation, and a compile of comment-only files proves no behavior. All relevant ticket check IDs must execute at their required layer with independent evidence. Services are not mocked; missing credentials/dependencies remain blockers.
