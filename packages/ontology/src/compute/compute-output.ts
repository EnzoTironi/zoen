// @zoen-plan packages/ontology/src/compute/compute-output.ts
// NON-EXECUTABLE PSEUDOCODE; not registered or compiled as product implementation.
// # File plan — `packages/ontology/src/compute/compute-output.ts`
//
// **Status:** planned; no product acceptance implied.
//
// Target: `packages/ontology/src/compute/compute-output.ts`. Representation: **comment-only-source**. Allocation: **required**.
//
// Specs: [SPEC-034](../../../../docs/specs/spec-034.md).
// Tickets: [ZN-0200](../../../../docs/tickets/zn-0200.md).
//
// ## Responsibility and reuse
//
// ```text
// PROCEDURE ZN_0200 /* planning label, not a public API */
//   OWNER := SPEC-034; TARGET := packages/ontology/src/compute/compute-output.ts
//   REQUIRE accepted dependencies: ZN-0199
//   REQUIRE evidence layer: component; actual admitted services when needed
//   IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
//   IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
//   USE the shared module protocol below; implement ONLY this ticket's segment, not a duplicate engine.
//     AUTHORIZE virtual-source/query or compute request under released definitions and current license/purpose.
//     PLAN bounded remote work and reserve cost/egress before dispatch; select only admitted actual profile.
//     PIN source/query/version/cut guarantees supported by provider; lack of snapshot coherence remains explicit.
//     BROKER credentials and egress outside guests; no arbitrary remote SQL from an app.
//     RUN tasks under fenced identity and record actual environment, inputs, partitions and result digests.
//     VALIDATE output schema, completeness and lineage before making a published artifact.
//     RETURN partial/unknown when provider guarantees or partitions are missing; retries preserve original task identity.
//     PUBLISH through common authority/dataset interfaces, never install a parallel distributed truth store.
//   TICKET-SPECIFIC SEGMENT:
//     01. Record exact engine/runtime/artifact/input versions and determinism declaration.
//     02. Enforce lineage/data-use labels on output and admit through normal dataset/analysis operations.
//     03. Capture estimated versus actual resources and stop runaway jobs.
//   TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
//     GIVEN A GPU model emits nondeterministic forecasts
//     WHEN Results are stored and compared across runs
//     THEN They are attributed analysis artifacts, not fabricated deterministic truth; each output has exact retained provenance and cost
//   ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
//   RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
// ```
//
// ## Acceptance boundary
//
// A plan is not implementation, and a compile of comment-only files proves no behavior. All relevant ticket check IDs must execute at their required layer with independent evidence. Services are not mocked; missing credentials/dependencies remain blockers.
