/** Authority module shared types (ZN-0019+). */
export type {
  MigrationPhase,
  SchemaDisciplineReport,
} from './schema.js';
export { MIGRATION_PHASES, AUTHORITY_TABLES, SCHEMA_ROLES } from './schema.js';
