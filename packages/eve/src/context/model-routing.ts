// @zoen-plan packages/eve/src/context/model-routing.ts
// NON-EXECUTABLE PSEUDOCODE; not registered or compiled as product implementation.
// # File plan — `packages/eve/src/context/model-routing.ts`
//
// **Status:** planned; no product acceptance implied.
//
// Target: `packages/eve/src/context/model-routing.ts`. Representation: **comment-only-source**. Allocation: **required**.
//
// Specs: [SPEC-010](../../../../docs/specs/spec-010.md).
// Tickets: [ZN-0060](../../../../docs/tickets/zn-0060.md).
//
// ## Responsibility and reuse
//
// ```text
// PROCEDURE ZN_0060 /* planning label, not a public API */
//   OWNER := SPEC-010; TARGET := packages/eve/src/context/model-routing.ts
//   REQUIRE accepted dependencies: ZN-0059
//   REQUIRE evidence layer: component; actual admitted services when needed
//   IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
//   IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
//   USE the shared module protocol below; implement ONLY this ticket's segment, not a duplicate engine.
//     LOAD only settled visible conversation events and authority-free Focus.
//     REOPEN every referenced Frame under current rights/purpose; missing context remains an explicit gap.
//     SEGREGATE retrieved source text as untrusted data, not executable instructions or tool policy.
//     INTERSECT released skill, current grant, app/workload scope and budget to determine tools.
//     SELECT only an admitted model/data-use/residency route with sufficient budget; otherwise NoPermittedModel.
//     COMPOSE bounded context with evidence references and provisional summaries; exclude hidden reasoning fields.
//     CAPTURE real provider outputs and usage; validate structured calls and send them to the same semantic executor.
//     TREAT audio transcription as an attributed candidate statement; consequential consent uses the normal confirmation path.
//   TICKET-SPECIFIC SEGMENT:
//     01. Admit provider schema adapters under separate lock profiles.
//     02. Evaluate data-use, region, cost and capability predicates before route selection.
//     03. Fail closed when no permitted route exists; preserve actual usage separately from estimate.
//   TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
//     GIVEN The preferred provider is unavailable and the fallback is outside the allowed data region
//     WHEN The router selects a model
//     THEN It returns NoPermittedModel rather than exporting data; an allowed fallback records its actual identity and usage
//   ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
//   RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
// ```
//
// ## Acceptance boundary
//
// A plan is not implementation, and a compile of comment-only files proves no behavior. All relevant ticket check IDs must execute at their required layer with independent evidence. Services are not mocked; missing credentials/dependencies remain blockers.
