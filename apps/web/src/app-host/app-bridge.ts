// @zoen-plan apps/web/src/app-host/app-bridge.ts
// NON-EXECUTABLE PSEUDOCODE; not registered or compiled as product implementation.
// # File plan — `apps/web/src/app-host/app-bridge.ts`
//
// **Status:** planned; no product acceptance implied.
//
// Target: `apps/web/src/app-host/app-bridge.ts`. Representation: **comment-only-source**. Allocation: **required**.
//
// Specs: [SPEC-035](../../../../docs/specs/spec-035.md).
// Tickets: [ZN-0204](../../../../docs/tickets/zn-0204.md).
//
// ## Responsibility and reuse
//
// ```text
// PROCEDURE ZN_0204 /* planning label, not a public API */
//   OWNER := SPEC-035; TARGET := apps/web/src/app-host/app-bridge.ts
//   REQUIRE accepted dependencies: ZN-0203, ZN-0311
//   REQUIRE evidence layer: component; actual admitted services when needed
//   IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
//   IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
//   USE the shared module protocol below; implement ONLY this ticket's segment, not a duplicate engine.
//     OPEN a non-authoritative builder draft against a known base release with current editor rights.
//     EDIT stable semantic definitions through ordinary operations; no privileged database console.
//     REUSE existing definition compiler, early declarative renderer, protected sessions and host bridge.
//     SHOW semantic, consequence, lineage, capability and data-impact diff before evaluation/publication.
//     PREVIEW only within isolated evaluation realm using authorized test data and sinks.
//     PUBLISH through current-policy release preparation/activation; agent authoring has no privileged mode.
//     REUSE signed-artifact and admitted runtime contract only when custom executable behavior is necessary.
//     PROVE full consumer/professional/institutional journeys without rebuilding S2/S3/S6 mechanisms or introducing a second data path.
//   TICKET-SPECIFIC SEGMENT:
//     01. Wire the SPEC-053 transport-only bridge and signed bundle origin into Living World and the app opener.
//     02. Reuse current AppSession and SemanticClient, with confirmations outside guest content.
//     03. Prove data operations enter the existing executor and isolate guest state; no app-specific policy/data handler.
//   TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
//     GIVEN A malicious app sends forged postMessage requests from another origin or asks for credentials
//     WHEN The host bridge receives them
//     THEN All are rejected; permitted operations return only authorized views
//   ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
//   RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
// ```
//
// ## Acceptance boundary
//
// A plan is not implementation, and a compile of comment-only files proves no behavior. All relevant ticket check IDs must execute at their required layer with independent evidence. Services are not mocked; missing credentials/dependencies remain blockers.
