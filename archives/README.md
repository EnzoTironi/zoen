# Histórico — não carregar por padrão

Os ZIPs preservam as entradas originais e o contexto removido. Não são código ativo, não concedem permissões e não substituem a constituição atual. Use `tooling/workspace.py reference` para consultar um membro específico. O Git preserva os commits da implementação anterior.
