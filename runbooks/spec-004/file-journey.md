# Two-source file journey repair — ZN-0030

**Status:** implementation-in-progress; ticket unaccepted.

```text
PRECHECK PG+MinIO, owner credentials, grandmother bill + user-message fixtures.
OBSERVE Frame retains both source_ids; payment assertion labeled unverified.
NEVER emit provider Settlement from a user "paid" claim without bank evidence.
ON duplicate upload: identical admission; on wrong-World: NotFoundOrDenied / no disclosure.
RESUME only after independent review.
```
