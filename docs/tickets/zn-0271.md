# ZN-0271 — Add evaluated stress and sensitivity analysis profiles

**Status:** planned/unaccepted · **Spec:** [SPEC-046](../specs/spec-046.md) · **Milestone:** S11 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-046.md](../specs/spec-046.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-046](../algorithms/spec-046.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0270](zn-0270.md).
External gate: `none on this ticket`. Resource locks: `dependency:admitted-lock`, `module:packs/finance/analytics`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0271 /* planning label, not a public API */
  OWNER := SPEC-046; TARGET := packs/finance/analytics/spec-046/risk-models.json
  REQUIRE accepted dependencies: ZN-0270
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Use isolated model/analysis artifacts with explicit assumptions, inputs and evaluation cases.
    02. Record numerical tolerance and stochastic/reproducibility limits.
    03. Keep scenarios read-only and fresh action proposal required.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN A stress model projects a favorable outcome under incomplete holdings
    WHEN An agent recommends execution
    THEN The analysis is labeled hypothetical/incomplete and cannot bypass the live risk/approval protocol
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packs/finance/analytics/spec-046/risk-models.json` | [sidecar-only](../../packs/finance/analytics/spec-046/risk-models.json.plan.md) | Primary |
| `tests/component/spec-046/risk-models.test.ts` | [comment-only-source](../../tests/component/spec-046/risk-models.test.ts) | Required tests |
| `runbooks/spec-046/risk-models.md` | [markdown-plan](../../runbooks/spec-046/risk-models.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packs/finance/analytics/spec-046/risk-models.json` — [plan](../../packs/finance/analytics/spec-046/risk-models.json.plan.md)
- `tests/component/spec-046/risk-models.test.ts` — [plan](../../tests/component/spec-046/risk-models.test.ts)
- `runbooks/spec-046/risk-models.md` — [plan](../../runbooks/spec-046/risk-models.md)
- `contracts/spec-046/risk-models.schema.json` — [plan](../../contracts/spec-046/risk-models.schema.json.plan.md)
- `tests/fixtures/spec-046/risk-models.json` — [plan](../../tests/fixtures/spec-046/risk-models.json.plan.md)
- `db/migrations/zn-0271_risk-models.sql` — [plan](../../db/migrations/zn-0271_risk-models.sql.plan.md)
- `tests/migrations/zn-0271.test.ts` — [plan](../../tests/migrations/zn-0271.test.ts)
- `pnpm-lock.yaml` — [plan](../../pnpm-lock.yaml.plan.md)
- `execution-lock.json` — [plan](../../execution-lock.json.plan.md)
- `admissions/spec-046/extension-lock.json` — [plan](../../admissions/spec-046/extension-lock.json.plan.md)
- `packs/finance/analytics/spec-046/pack.json` — [plan](../../packs/finance/analytics/spec-046/pack.json.plan.md)

## Required tests

```text
SUITE ZN-0271 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0271-AC:
    ARRANGE A stress model projects a favorable outcome under incomplete holdings
    ACT An agent recommends execution
    ASSERT The analysis is labeled hypothetical/incomplete and cannot bypass the live risk/approval protocol
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0271-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Use stale/gapped data or an unqualified model outside its licensed scope. Risk output is unavailable/unqualified, not falsely precise. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0271-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Repeat with duplicate/reordered input, revoked access and the profile limit at the task boundary. Preserve the declared oracle; report unsupported/incomplete state rather than silent truncation, disclosure or fabricated success.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0271 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
