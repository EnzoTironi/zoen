# ZN-0103 — Keep five reply meanings distinct

**Status:** planned/unaccepted · **Spec:** [SPEC-017](../specs/spec-017.md) · **Milestone:** S2 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-017.md](../specs/spec-017.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-017](../algorithms/spec-017.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0102](zn-0102.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/ontology/src/stewardship`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0103 /* planning label, not a public API */
  OWNER := SPEC-017; TARGET := packages/ontology/src/stewardship/reply-kinds.ts
  REQUIRE accepted dependencies: ZN-0102
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Validate the reply kind and bind question digest/basis.
    02. Write assertions/identity/instance changes through their existing protocols.
    03. Create only a draft for reusable-rule intent, and route preferences to interaction settings.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN The user says for this order use 100 and later always prefer signed amendments
    WHEN Both replies are handled
    THEN The first affects one instance; the second creates a separate DefinitionChange requiring evaluation/approval, not an immediate hidden policy edit
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/stewardship/reply-kinds.ts` | [comment-only-source](../../packages/ontology/src/stewardship/reply-kinds.ts) | Primary |
| `tests/component/spec-017/reply-kinds.test.ts` | [comment-only-source](../../tests/component/spec-017/reply-kinds.test.ts) | Required tests |
| `runbooks/spec-017/reply-kinds.md` | [markdown-plan](../../runbooks/spec-017/reply-kinds.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/stewardship/reply-kinds.ts` — [plan](../../packages/ontology/src/stewardship/reply-kinds.ts)
- `tests/component/spec-017/reply-kinds.test.ts` — [plan](../../tests/component/spec-017/reply-kinds.test.ts)
- `runbooks/spec-017/reply-kinds.md` — [plan](../../runbooks/spec-017/reply-kinds.md)
- `contracts/spec-017/reply-kinds.schema.json` — [plan](../../contracts/spec-017/reply-kinds.schema.json.plan.md)
- `tests/fixtures/spec-017/reply-kinds.json` — [plan](../../tests/fixtures/spec-017/reply-kinds.json.plan.md)
- `db/migrations/zn-0103_reply-kinds.sql` — [plan](../../db/migrations/zn-0103_reply-kinds.sql.plan.md)
- `tests/migrations/zn-0103.test.ts` — [plan](../../tests/migrations/zn-0103.test.ts)
- `packages/ontology/src/stewardship/index.ts` — [plan](../../packages/ontology/src/stewardship/index.ts)
- `packages/ontology/src/stewardship/ports.ts` — [plan](../../packages/ontology/src/stewardship/ports.ts)
- `packages/ontology/src/stewardship/types.ts` — [plan](../../packages/ontology/src/stewardship/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-017.ts` — [plan](../../apps/authority-worker/src/registrations/spec-017.ts)

## Required tests

```text
SUITE ZN-0103 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0103-AC:
    ARRANGE The user says for this order use 100 and later always prefer signed amendments
    ACT Both replies are handled
    ASSERT The first affects one instance; the second creates a separate DefinitionChange requiring evaluation/approval, not an immediate hidden policy edit
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0103-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Submit an answer for the wrong question digest/kind/scope. No local or reusable knowledge changes. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0103-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT For this operation, race duplicate delivery/replay and a relevant dependency change at a named commit boundary. At most one same-intent semantic result commits; stale work is rejected. For read-only/validation work, prove deterministic output at the same basis and explicit stale/unsupported output at the changed basis.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-23: barrier=after wrong scoped reply then undo; inject=Replay prior question and correction IDs; assert=History preserved, no global rule installation.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0103 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
