# ZN-0064 — Admit signed webhooks before acknowledgement

**Status:** planned/unaccepted · **Spec:** [SPEC-011](../specs/spec-011.md) · **Milestone:** S1 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-011.md](../specs/spec-011.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [docs/architecture/protected-links.md](../architecture/protected-links.md)
- [algorithm SPEC-011](../algorithms/spec-011.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0057](zn-0057.md), [ZN-0062](zn-0062.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `dependency:admitted-lock`, `module:packages/adapters/src/channels/whatsapp`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0064 /* planning label, not a public API */
  OWNER := SPEC-011; TARGET := packages/adapters/src/channels/whatsapp/webhook.ts
  REQUIRE accepted dependencies: ZN-0057, ZN-0062
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Implement versioned provider verification against published fixtures.
    02. Preserve stable provider/account/event identity and replay protection.
    03. Atomically write channel ingress and outbox before returning provider success.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN Duplicate signed webhook and a process kill immediately before/after durable admission
    WHEN The provider retries
    THEN Valid ingress is accepted once; invalid signatures are rejected; no success ACK precedes durability
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/adapters/src/channels/whatsapp/webhook.ts` | [comment-only-source](../../packages/adapters/src/channels/whatsapp/webhook.ts) | Primary |
| `tests/component/spec-011/webhook.test.ts` | [comment-only-source](../../tests/component/spec-011/webhook.test.ts) | Required tests |
| `runbooks/spec-011/webhook.md` | [markdown-plan](../../runbooks/spec-011/webhook.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/adapters/src/channels/whatsapp/webhook.ts` — [plan](../../packages/adapters/src/channels/whatsapp/webhook.ts)
- `tests/component/spec-011/webhook.test.ts` — [plan](../../tests/component/spec-011/webhook.test.ts)
- `runbooks/spec-011/webhook.md` — [plan](../../runbooks/spec-011/webhook.md)
- `contracts/spec-011/webhook.schema.json` — [plan](../../contracts/spec-011/webhook.schema.json.plan.md)
- `tests/fixtures/spec-011/webhook.json` — [plan](../../tests/fixtures/spec-011/webhook.json.plan.md)
- `db/migrations/zn-0064_webhook.sql` — [plan](../../db/migrations/zn-0064_webhook.sql.plan.md)
- `tests/migrations/zn-0064.test.ts` — [plan](../../tests/migrations/zn-0064.test.ts)
- `pnpm-lock.yaml` — [plan](../../pnpm-lock.yaml.plan.md)
- `execution-lock.json` — [plan](../../execution-lock.json.plan.md)
- `admissions/spec-011/extension-lock.json` — [plan](../../admissions/spec-011/extension-lock.json.plan.md)
- `packages/adapters/package.json` — [plan](../../packages/adapters/package.json.plan.md)
- `packages/adapters/src/channels/whatsapp/index.ts` — [plan](../../packages/adapters/src/channels/whatsapp/index.ts)
- `packages/adapters/src/channels/whatsapp/ports.ts` — [plan](../../packages/adapters/src/channels/whatsapp/ports.ts)
- `packages/adapters/src/channels/whatsapp/types.ts` — [plan](../../packages/adapters/src/channels/whatsapp/types.ts)
- `apps/edge/src/composition.ts` — [plan](../../apps/edge/src/composition.ts)

## Required tests

```text
SUITE ZN-0064 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0064-AC:
    ARRANGE Duplicate signed webhook and a process kill immediately before/after durable admission
    ACT The provider retries
    ASSERT Valid ingress is accepted once; invalid signatures are rejected; no success ACK precedes durability
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0064-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Submit unverified provider ingress or bind a number without the required proof/consent. No identity or protected reply is created. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0064-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Interrupt after durable admission or provider acceptance but before acknowledgement. Retry with the same identity and an old worker fence. No duplicate committed result or unsafe resend occurs; ambiguity stays Unknown until observed.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-06: barrier=after inbox/outbox commit, before ACK; inject=Provider redelivery and process restart; assert=One admitted inbound message; valid retry safe.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0064 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
