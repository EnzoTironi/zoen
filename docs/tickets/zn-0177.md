# ZN-0177 — Implement Python, R and SQL read-only analysis profiles

**Status:** planned/unaccepted · **Spec:** [SPEC-030](../specs/spec-030.md) · **Milestone:** S6 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-030.md](../specs/spec-030.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [algorithm SPEC-030](../algorithms/spec-030.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0176](zn-0176.md).
External gate: `none on this ticket`. Resource locks: `dependency:admitted-lock`, `module:runners`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0177 /* planning label, not a public API */
  OWNER := SPEC-030; TARGET := runners/analysis-profiles.ts
  REQUIRE accepted dependencies: ZN-0176
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Mount only immutable leased inputs and output staging areas.
    02. Record exact runtime/library lock, source code, seed and declared nondeterminism.
    03. Return an AnalysisArtifact with lineage; do not write facts or call Actions directly.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN A notebook computes a forecast and attempts direct database write/network access
    WHEN The analysis runner executes it
    THEN The forecast is a read-only attributed artifact; unauthorized writes/network fail and no prediction becomes a fact automatically
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `runners/analysis-profiles.ts` | [comment-only-source](../../runners/analysis-profiles.ts) | Primary |
| `tests/component/spec-030/analysis-profiles.test.ts` | [comment-only-source](../../tests/component/spec-030/analysis-profiles.test.ts) | Required tests |
| `runbooks/spec-030/analysis-profiles.md` | [markdown-plan](../../runbooks/spec-030/analysis-profiles.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `runners/analysis-profiles.ts` — [plan](../../runners/analysis-profiles.ts)
- `tests/component/spec-030/analysis-profiles.test.ts` — [plan](../../tests/component/spec-030/analysis-profiles.test.ts)
- `runbooks/spec-030/analysis-profiles.md` — [plan](../../runbooks/spec-030/analysis-profiles.md)
- `contracts/spec-030/analysis-profiles.schema.json` — [plan](../../contracts/spec-030/analysis-profiles.schema.json.plan.md)
- `tests/fixtures/spec-030/analysis-profiles.json` — [plan](../../tests/fixtures/spec-030/analysis-profiles.json.plan.md)
- `db/migrations/zn-0177_analysis-profiles.sql` — [plan](../../db/migrations/zn-0177_analysis-profiles.sql.plan.md)
- `tests/migrations/zn-0177.test.ts` — [plan](../../tests/migrations/zn-0177.test.ts)
- `pnpm-lock.yaml` — [plan](../../pnpm-lock.yaml.plan.md)
- `execution-lock.json` — [plan](../../execution-lock.json.plan.md)
- `admissions/spec-030/extension-lock.json` — [plan](../../admissions/spec-030/extension-lock.json.plan.md)
- `runners/index.ts` — [plan](../../runners/index.ts)
- `runners/ports.ts` — [plan](../../runners/ports.ts)
- `runners/types.ts` — [plan](../../runners/types.ts)
- `runners/python/entry.py` — [plan](../../runners/python/entry.py)
- `runners/r/entry.R` — [plan](../../runners/r/entry.R)
- `runners/sql/entry.ts` — [plan](../../runners/sql/entry.ts)
- `runners/oci/Dockerfile` — [plan](../../runners/oci/Dockerfile.plan.md)
- `runners/oci/profile.json` — [plan](../../runners/oci/profile.json.plan.md)

## Required tests

```text
SUITE ZN-0177 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0177-AC:
    ARRANGE A notebook computes a forecast and attempts direct database write/network access
    ACT The analysis runner executes it
    ASSERT The forecast is a read-only attributed artifact; unauthorized writes/network fail and no prediction becomes a fact automatically
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0177-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Probe host metadata, Docker sockets, authority credentials or undeclared network destinations. The actual isolation boundary denies access. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0177-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Terminate execution after output staging or resource exhaustion. Leases expire, unadmitted output stays non-authoritative, and no resumed sandbox acquires broader authority.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-12: barrier=before secret/network/output access; inject=Actual guest host/metadata/egress probe; assert=Denied by infrastructure/broker, not merely guest convention.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0177 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
