# ZN-0032 — Collapse dependent source families without deleting provenance

**Status:** implemented/unaccepted · **Spec:** [SPEC-005](../specs/spec-005.md) · **Milestone:** S0 · **Layer:** law · **Risk:** standard

## Read set

- [docs/specs/spec-005.md](../specs/spec-005.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-005](../algorithms/spec-005.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0031](zn-0031.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/ontology/src/interpretation`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0032 /* planning label, not a public API */
  OWNER := SPEC-005; TARGET := packages/ontology/src/interpretation/families.ts
  REQUIRE accepted dependencies: ZN-0031
  REQUIRE evidence layer: law; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Track original and copied/derived evidence ancestry.
    02. Collapse independent support counting by source family and reject self-supporting derivation cycles.
    03. Keep every permitted raw artifact visible as lineage.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN One original claim copied into ten documents and one independent conflicting source
    WHEN The interpretation computes support
    THEN The copies contribute one independent family, not ten votes; all eleven evidence links remain attributable
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/interpretation/families.ts` | [comment-only-source](../../packages/ontology/src/interpretation/families.ts) | Primary |
| `tests/law/spec-005/families.test.ts` | [comment-only-source](../../tests/law/spec-005/families.test.ts) | Required tests |
| `runbooks/spec-005/families.md` | [markdown-plan](../../runbooks/spec-005/families.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/interpretation/families.ts` — [plan](../../packages/ontology/src/interpretation/families.ts)
- `tests/law/spec-005/families.test.ts` — [plan](../../tests/law/spec-005/families.test.ts)
- `runbooks/spec-005/families.md` — [plan](../../runbooks/spec-005/families.md)
- `contracts/spec-005/families.schema.json` — [plan](../../contracts/spec-005/families.schema.json.plan.md)
- `tests/fixtures/spec-005/families.json` — [plan](../../tests/fixtures/spec-005/families.json.plan.md)
- `packages/ontology/src/interpretation/index.ts` — [plan](../../packages/ontology/src/interpretation/index.ts)
- `packages/ontology/src/interpretation/ports.ts` — [plan](../../packages/ontology/src/interpretation/ports.ts)
- `packages/ontology/src/interpretation/types.ts` — [plan](../../packages/ontology/src/interpretation/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-005.ts` — [plan](../../apps/authority-worker/src/registrations/spec-005.ts)

## Required tests

```text
SUITE ZN-0032 [required layer=law; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0032-AC:
    ARRANGE One original claim copied into ten documents and one independent conflicting source
    ACT The interpretation computes support
    ASSERT The copies contribute one independent family, not ten votes; all eleven evidence links remain attributable
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0032-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Supply an invalid type, incompatible unit/time/realm or unsupported scalar to this task’s pure boundary. It returns the specified tagged error without coercion or I/O.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0032-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Exercise empty/minimum/maximum/overflow and reordered equivalent inputs using recorded seeds. Exact semantics are deterministic; unsupported limits yield explicit errors, not truncation.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0032 --profile <admitted-law-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
