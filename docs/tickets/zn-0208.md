# ZN-0208 — Compare scenarios with explicit basis and coverage

**Status:** planned/unaccepted · **Spec:** [SPEC-036](../specs/spec-036.md) · **Milestone:** S8 · **Layer:** component · **Risk:** standard

## Read set

- [docs/specs/spec-036.md](../specs/spec-036.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-036](../algorithms/spec-036.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0207](zn-0207.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/ontology/src/scenarios`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0208 /* planning label, not a public API */
  OWNER := SPEC-036; TARGET := packages/ontology/src/scenarios/scenario-compare.ts
  REQUIRE accepted dependencies: ZN-0207
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Use shared metric definitions and normalized units/time basis.
    02. Show differing assumptions, source cuts and missing partitions.
    03. Reject misleading comparison across incompatible metric semantics.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN Two revenue scenarios use bookings versus cash receipts
    WHEN A comparison is requested
    THEN The system explains incompatible metrics rather than presenting a misleading single winner
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/scenarios/scenario-compare.ts` | [comment-only-source](../../packages/ontology/src/scenarios/scenario-compare.ts) | Primary |
| `tests/component/spec-036/scenario-compare.test.ts` | [comment-only-source](../../tests/component/spec-036/scenario-compare.test.ts) | Required tests |
| `runbooks/spec-036/scenario-compare.md` | [markdown-plan](../../runbooks/spec-036/scenario-compare.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/scenarios/scenario-compare.ts` — [plan](../../packages/ontology/src/scenarios/scenario-compare.ts)
- `tests/component/spec-036/scenario-compare.test.ts` — [plan](../../tests/component/spec-036/scenario-compare.test.ts)
- `runbooks/spec-036/scenario-compare.md` — [plan](../../runbooks/spec-036/scenario-compare.md)
- `contracts/spec-036/scenario-compare.schema.json` — [plan](../../contracts/spec-036/scenario-compare.schema.json.plan.md)
- `tests/fixtures/spec-036/scenario-compare.json` — [plan](../../tests/fixtures/spec-036/scenario-compare.json.plan.md)
- `db/migrations/zn-0208_scenario-compare.sql` — [plan](../../db/migrations/zn-0208_scenario-compare.sql.plan.md)
- `tests/migrations/zn-0208.test.ts` — [plan](../../tests/migrations/zn-0208.test.ts)
- `packages/ontology/src/scenarios/index.ts` — [plan](../../packages/ontology/src/scenarios/index.ts)
- `packages/ontology/src/scenarios/ports.ts` — [plan](../../packages/ontology/src/scenarios/ports.ts)
- `packages/ontology/src/scenarios/types.ts` — [plan](../../packages/ontology/src/scenarios/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-036.ts` — [plan](../../apps/authority-worker/src/registrations/spec-036.ts)

## Required tests

```text
SUITE ZN-0208 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0208-AC:
    ARRANGE Two revenue scenarios use bookings versus cash receipts
    ACT A comparison is requested
    ASSERT The system explains incompatible metrics rather than presenting a misleading single winner
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0208-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT A scenario or notebook attempts a live effect or trains across unauthorized tenants. It has no such authority. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0208-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Terminate execution after output staging or resource exhaustion. Leases expire, unadmitted output stays non-authoritative, and no resumed sandbox acquires broader authority.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0208 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
