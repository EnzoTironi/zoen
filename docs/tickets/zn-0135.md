# ZN-0135 — Admit Restate and narrow durable handler contract

**Status:** planned/unaccepted · **Spec:** [SPEC-023](../specs/spec-023.md) · **Milestone:** S4 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-023.md](../specs/spec-023.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [algorithm SPEC-023](../algorithms/spec-023.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0068](zn-0068.md), [ZN-0134](zn-0134.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `dependency:admitted-lock`, `module:packages/ontology/src/effects`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0135 /* planning label, not a public API */
  OWNER := SPEC-023; TARGET := packages/ontology/src/effects/restate-adapter.ts
  REQUIRE accepted dependencies: ZN-0068, ZN-0134
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Pin server/SDK versions and supported deployment profile.
    02. Register only the EffectIntent handler with workload authentication.
    03. Configure retry/error behavior so unsafe ambiguous sends are not repeated blindly.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN The handler receives an unrecognized intent and an unsafe operation that timed out after transmit
    WHEN Restate resumes the invocation
    THEN Unknown intents are denied and the ambiguous send enters reconciliation rather than unconditional retry
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/effects/restate-adapter.ts` | [comment-only-source](../../packages/ontology/src/effects/restate-adapter.ts) | Primary |
| `tests/component/spec-023/restate-adapter.test.ts` | [comment-only-source](../../tests/component/spec-023/restate-adapter.test.ts) | Required tests |
| `runbooks/spec-023/restate-adapter.md` | [markdown-plan](../../runbooks/spec-023/restate-adapter.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/effects/restate-adapter.ts` — [plan](../../packages/ontology/src/effects/restate-adapter.ts)
- `tests/component/spec-023/restate-adapter.test.ts` — [plan](../../tests/component/spec-023/restate-adapter.test.ts)
- `runbooks/spec-023/restate-adapter.md` — [plan](../../runbooks/spec-023/restate-adapter.md)
- `contracts/spec-023/restate-adapter.schema.json` — [plan](../../contracts/spec-023/restate-adapter.schema.json.plan.md)
- `tests/fixtures/spec-023/restate-adapter.json` — [plan](../../tests/fixtures/spec-023/restate-adapter.json.plan.md)
- `db/migrations/zn-0135_restate-adapter.sql` — [plan](../../db/migrations/zn-0135_restate-adapter.sql.plan.md)
- `tests/migrations/zn-0135.test.ts` — [plan](../../tests/migrations/zn-0135.test.ts)
- `pnpm-lock.yaml` — [plan](../../pnpm-lock.yaml.plan.md)
- `execution-lock.json` — [plan](../../execution-lock.json.plan.md)
- `admissions/spec-023/extension-lock.json` — [plan](../../admissions/spec-023/extension-lock.json.plan.md)
- `packages/ontology/package.json` — [plan](../../packages/ontology/package.json.plan.md)
- `packages/ontology/src/effects/index.ts` — [plan](../../packages/ontology/src/effects/index.ts)
- `packages/ontology/src/effects/ports.ts` — [plan](../../packages/ontology/src/effects/ports.ts)
- `packages/ontology/src/effects/types.ts` — [plan](../../packages/ontology/src/effects/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-023.ts` — [plan](../../apps/authority-worker/src/registrations/spec-023.ts)
- `apps/effect-worker/src/composition.ts` — [plan](../../apps/effect-worker/src/composition.ts)

## Required tests

```text
SUITE ZN-0135 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0135-AC:
    ARRANGE The handler receives an unrecognized intent and an unsafe operation that timed out after transmit
    ACT Restate resumes the invocation
    ASSERT Unknown intents are denied and the ambiguous send enters reconciliation rather than unconditional retry
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0135-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Retry an escaped effect with Unknown outcome or a revoked execution permit. No unsafe duplicate external call is made. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0135-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Interrupt after durable admission or provider acceptance but before acknowledgement. Retry with the same identity and an old worker fence. No duplicate committed result or unsafe resend occurs; ambiguity stays Unknown until observed.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-09: barrier=provider accepts, reply lost; inject=Timeout then repeated durable job; assert=Unknown preserved; no blind unsafe resend.
    F-10: barrier=cancel requested while provider accepts; inject=Reorder acceptance/cancel callbacks; assert=Local stop separate from escaped outcome.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0135 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
