# ZN-0256 — Record offline operations and enforce expiry

**Status:** planned/unaccepted · **Spec:** [SPEC-044](../specs/spec-044.md) · **Milestone:** S10 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-044.md](../specs/spec-044.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [algorithm SPEC-044](../algorithms/spec-044.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0255](zn-0255.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/ontology/src/edge`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0256 /* planning label, not a public API */
  OWNER := SPEC-044; TARGET := packages/ontology/src/edge/offline-execution.ts
  REQUIRE accepted dependencies: ZN-0255
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Maintain local ordered receipts with lease identity and uncertainty.
    02. Stop when expiry/clock uncertainty prevents proving validity.
    03. Disable non-reversible effects unless the lease explicitly partitions the needed authority/resource.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN A device clock rolls back and its lease may have expired
    WHEN It attempts a consequential operation
    THEN The operation is denied/escalated rather than exploiting the clock change to extend authority
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/edge/offline-execution.ts` | [comment-only-source](../../packages/ontology/src/edge/offline-execution.ts) | Primary |
| `tests/component/spec-044/offline-execution.test.ts` | [comment-only-source](../../tests/component/spec-044/offline-execution.test.ts) | Required tests |
| `runbooks/spec-044/offline-execution.md` | [markdown-plan](../../runbooks/spec-044/offline-execution.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/edge/offline-execution.ts` — [plan](../../packages/ontology/src/edge/offline-execution.ts)
- `tests/component/spec-044/offline-execution.test.ts` — [plan](../../tests/component/spec-044/offline-execution.test.ts)
- `runbooks/spec-044/offline-execution.md` — [plan](../../runbooks/spec-044/offline-execution.md)
- `contracts/spec-044/offline-execution.schema.json` — [plan](../../contracts/spec-044/offline-execution.schema.json.plan.md)
- `tests/fixtures/spec-044/offline-execution.json` — [plan](../../tests/fixtures/spec-044/offline-execution.json.plan.md)
- `db/migrations/zn-0256_offline-execution.sql` — [plan](../../db/migrations/zn-0256_offline-execution.sql.plan.md)
- `tests/migrations/zn-0256.test.ts` — [plan](../../tests/migrations/zn-0256.test.ts)
- `packages/ontology/src/edge/index.ts` — [plan](../../packages/ontology/src/edge/index.ts)
- `packages/ontology/src/edge/ports.ts` — [plan](../../packages/ontology/src/edge/ports.ts)
- `packages/ontology/src/edge/types.ts` — [plan](../../packages/ontology/src/edge/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-044.ts` — [plan](../../apps/authority-worker/src/registrations/spec-044.ts)

## Required tests

```text
SUITE ZN-0256 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0256-AC:
    ARRANGE A device clock rolls back and its lease may have expired
    ACT It attempts a consequential operation
    ASSERT The operation is denied/escalated rather than exploiting the clock change to extend authority
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0256-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT An expired offline lease or edge artifact requests live secret/authority reuse. Deny and retain pending local work separately. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0256-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Inject source/process/network loss during rollout or restore. Reject unsafe writes until required fencing and recovery evidence exists; preserve explicit partial or unknown external outcomes.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-19: barrier=after lease expiry before sync; inject=Reconnect stale edge and replay actions; assert=No revived authority; pending local claims reviewed normally.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0256 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
