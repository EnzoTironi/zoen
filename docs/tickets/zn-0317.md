# ZN-0317 — Harden Rivet host configuration, build plane and credentials

**Status:** planned/unaccepted · **Spec:** [SPEC-054](../specs/spec-054.md) · **Milestone:** S6 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-054.md](../specs/spec-054.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [docs/architecture/rivet-adapter.md](../architecture/rivet-adapter.md)
- [docs/architecture/app-host-security.md](../architecture/app-host-security.md)
- [algorithm SPEC-054](../algorithms/spec-054.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0176](zn-0176.md), [ZN-0311](zn-0311.md), [ZN-0316](zn-0316.md).
External gate: `none on this ticket`. Resource locks: `infra:app-host`, `module:packages/adapters/src/app-runtime/rivet`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0317 /* planning label, not a public API */
  OWNER := SPEC-054; TARGET := packages/adapters/src/app-runtime/rivet/rivet-host-profile.ts
  REQUIRE accepted dependencies: ZN-0176, ZN-0311, ZN-0316
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Generate trusted VM/namespace config only from admitted policy; never merge untrusted manifest permissions into host config.
    02. Run backend executor outside the authority process under qualified external containment; broker the semantic client only.
    03. Disable default production mounts, registry secrets and unrestricted network; sanitize request/response headers and logs.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN Hostile generated code attempts environment reads, SSRF, state reuse and expensive work
    WHEN It runs under the exact proposed Rivet runtime profile
    THEN No authority/source credentials or cross-user state are obtainable; CPU/memory/output/egress limits are externally enforced and observable
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/adapters/src/app-runtime/rivet/rivet-host-profile.ts` | [comment-only-source](../../packages/adapters/src/app-runtime/rivet/rivet-host-profile.ts) | Primary |
| `tests/component/spec-054/rivet-host-profile.test.ts` | [comment-only-source](../../tests/component/spec-054/rivet-host-profile.test.ts) | Required tests |
| `runbooks/spec-054/rivet-host-profile.md` | [markdown-plan](../../runbooks/spec-054/rivet-host-profile.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/adapters/src/app-runtime/rivet/rivet-host-profile.ts` — [plan](../../packages/adapters/src/app-runtime/rivet/rivet-host-profile.ts)
- `tests/component/spec-054/rivet-host-profile.test.ts` — [plan](../../tests/component/spec-054/rivet-host-profile.test.ts)
- `runbooks/spec-054/rivet-host-profile.md` — [plan](../../runbooks/spec-054/rivet-host-profile.md)
- `contracts/spec-054/rivet-host-profile.schema.json` — [plan](../../contracts/spec-054/rivet-host-profile.schema.json.plan.md)
- `tests/fixtures/spec-054/rivet-host-profile.json` — [plan](../../tests/fixtures/spec-054/rivet-host-profile.json.plan.md)
- `db/migrations/zn-0317_rivet-host-profile.sql` — [plan](../../db/migrations/zn-0317_rivet-host-profile.sql.plan.md)
- `tests/migrations/zn-0317.test.ts` — [plan](../../tests/migrations/zn-0317.test.ts)
- `packages/adapters/src/app-runtime/rivet/index.ts` — [plan](../../packages/adapters/src/app-runtime/rivet/index.ts)
- `packages/adapters/src/app-runtime/rivet/ports.ts` — [plan](../../packages/adapters/src/app-runtime/rivet/ports.ts)
- `packages/adapters/src/app-runtime/rivet/types.ts` — [plan](../../packages/adapters/src/app-runtime/rivet/types.ts)
- `runners/apps/rivet-entry.ts` — [plan](../../runners/apps/rivet-entry.ts)
- `runners/apps/rivet-profile.json` — [plan](../../runners/apps/rivet-profile.json.plan.md)
- `runners/oci/Dockerfile` — [plan](../../runners/oci/Dockerfile.plan.md)
- `infra/terraform/app-host/main.tf` — [plan](../../infra/terraform/app-host/main.tf.plan.md)

## Required tests

```text
SUITE ZN-0317 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0317-AC:
    ARRANGE Hostile generated code attempts environment reads, SSRF, state reuse and expensive work
    ACT It runs under the exact proposed Rivet runtime profile
    ASSERT No authority/source credentials or cross-user state are obtainable; CPU/memory/output/egress limits are externally enforced and observable
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0317-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT An agent-provided network allow rule, VM mount, namespace key or dependency install script cannot widen the host policy
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0317-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Warm VM replay or container restart checks the same realm/session binding and cannot revive a recalled installation
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-30: barrier=after internal deploy before proof write; inject=Kill runtime host; assert=Reconcile immutable slot; public binding unchanged.
    F-31: barrier=after prepare before activation; inject=Revoke author or advance release head; assert=Denied/PreparationStale; no public promotion.
    F-32: barrier=after recall before routing cache invalidation; inject=Send request to stale host; assert=Current recall blocks data/calls; no latest fallback.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0317 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
