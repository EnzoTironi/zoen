# ZN-0273 — Implement order submission and acknowledged lifecycle

**Status:** planned/unaccepted · **Spec:** [SPEC-047](../specs/spec-047.md) · **Milestone:** S11 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-047.md](../specs/spec-047.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-047](../algorithms/spec-047.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0117](zn-0117.md), [ZN-0139](zn-0139.md), [ZN-0254](zn-0254.md), [ZN-0271](zn-0271.md).
External gate: `none on this ticket`. Resource locks: `module:packs/finance/execution`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0273 /* planning label, not a public API */
  OWNER := SPEC-047; TARGET := packs/finance/execution/spec-047/order-lifecycle.json
  REQUIRE accepted dependencies: ZN-0117, ZN-0139, ZN-0254, ZN-0271
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Define proposed/approved/submitted/acknowledged/working/partial/filled/cancelled/expired/rejected/unknown observations.
    02. Route submission through guarded Action and admitted broker effect protocol.
    03. Preserve intent identity across reconnect/reconciliation.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN Broker accepts an order but the submission response is lost
    WHEN The worker resumes
    THEN The order remains unknown until reconciled and cannot be blindly submitted again with a new client order identity
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packs/finance/execution/spec-047/order-lifecycle.json` | [sidecar-only](../../packs/finance/execution/spec-047/order-lifecycle.json.plan.md) | Primary |
| `tests/component/spec-047/order-lifecycle.test.ts` | [comment-only-source](../../tests/component/spec-047/order-lifecycle.test.ts) | Required tests |
| `runbooks/spec-047/order-lifecycle.md` | [markdown-plan](../../runbooks/spec-047/order-lifecycle.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packs/finance/execution/spec-047/order-lifecycle.json` — [plan](../../packs/finance/execution/spec-047/order-lifecycle.json.plan.md)
- `tests/component/spec-047/order-lifecycle.test.ts` — [plan](../../tests/component/spec-047/order-lifecycle.test.ts)
- `runbooks/spec-047/order-lifecycle.md` — [plan](../../runbooks/spec-047/order-lifecycle.md)
- `contracts/spec-047/order-lifecycle.schema.json` — [plan](../../contracts/spec-047/order-lifecycle.schema.json.plan.md)
- `tests/fixtures/spec-047/order-lifecycle.json` — [plan](../../tests/fixtures/spec-047/order-lifecycle.json.plan.md)
- `db/migrations/zn-0273_order-lifecycle.sql` — [plan](../../db/migrations/zn-0273_order-lifecycle.sql.plan.md)
- `tests/migrations/zn-0273.test.ts` — [plan](../../tests/migrations/zn-0273.test.ts)
- `packs/finance/execution/spec-047/pack.json` — [plan](../../packs/finance/execution/spec-047/pack.json.plan.md)

## Required tests

```text
SUITE ZN-0273 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0273-AC:
    ARRANGE Broker accepts an order but the submission response is lost
    ACT The worker resumes
    ASSERT The order remains unknown until reconciled and cannot be blindly submitted again with a new client order identity
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0273-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Treat broker acknowledgement/cancel request as settlement or blindly resubmit an Unknown order. Reject that transition and keep exposure reserved. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0273-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Interrupt after durable admission or provider acceptance but before acknowledgement. Retry with the same identity and an old worker fence. No duplicate committed result or unsafe resend occurs; ambiguity stays Unknown until observed.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-20: barrier=fill/bust/cancel/allocation callbacks; inject=Duplicate and permute verified broker events; assert=Quantity conservation, no inferred custody settlement.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0273 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
