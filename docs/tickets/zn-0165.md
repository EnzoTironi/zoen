# ZN-0165 — Implement email ingress, threading and attachment safety

**Status:** planned/unaccepted · **Spec:** [SPEC-028](../specs/spec-028.md) · **Milestone:** S5 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-028.md](../specs/spec-028.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-028](../algorithms/spec-028.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0164](zn-0164.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/adapters/src/channels/multichannel`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0165 /* planning label, not a public API */
  OWNER := SPEC-028; TARGET := packages/adapters/src/channels/multichannel/email.ts
  REQUIRE accepted dependencies: ZN-0164
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Verify provider event signatures over raw payloads.
    02. Preserve message/thread identity without treating From as authenticated authority.
    03. Reject header injection and route attachments through evidence quarantine.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN A spoofed From header names the World owner and asks for a confidential export
    WHEN The email is admitted as transport input
    THEN No owner grant is inferred; confidential action remains denied pending secure identity/approval
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/adapters/src/channels/multichannel/email.ts` | [comment-only-source](../../packages/adapters/src/channels/multichannel/email.ts) | Primary |
| `tests/component/spec-028/email.test.ts` | [comment-only-source](../../tests/component/spec-028/email.test.ts) | Required tests |
| `runbooks/spec-028/email.md` | [markdown-plan](../../runbooks/spec-028/email.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/adapters/src/channels/multichannel/email.ts` — [plan](../../packages/adapters/src/channels/multichannel/email.ts)
- `tests/component/spec-028/email.test.ts` — [plan](../../tests/component/spec-028/email.test.ts)
- `runbooks/spec-028/email.md` — [plan](../../runbooks/spec-028/email.md)
- `contracts/spec-028/email.schema.json` — [plan](../../contracts/spec-028/email.schema.json.plan.md)
- `tests/fixtures/spec-028/email.json` — [plan](../../tests/fixtures/spec-028/email.json.plan.md)
- `db/migrations/zn-0165_email.sql` — [plan](../../db/migrations/zn-0165_email.sql.plan.md)
- `tests/migrations/zn-0165.test.ts` — [plan](../../tests/migrations/zn-0165.test.ts)
- `packages/adapters/src/channels/multichannel/index.ts` — [plan](../../packages/adapters/src/channels/multichannel/index.ts)
- `packages/adapters/src/channels/multichannel/ports.ts` — [plan](../../packages/adapters/src/channels/multichannel/ports.ts)
- `packages/adapters/src/channels/multichannel/types.ts` — [plan](../../packages/adapters/src/channels/multichannel/types.ts)
- `apps/edge/src/composition.ts` — [plan](../../apps/edge/src/composition.ts)

## Required tests

```text
SUITE ZN-0165 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0165-AC:
    ARRANGE A spoofed From header names the World owner and asks for a confidential export
    ACT The email is admitted as transport input
    ASSERT No owner grant is inferred; confidential action remains denied pending secure identity/approval
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0165-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Treat email From, Telegram group presence or an old channel binding as proof of identity. No authority is inherited. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0165-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Interrupt after durable admission or provider acceptance but before acknowledgement. Retry with the same identity and an old worker fence. No duplicate committed result or unsafe resend occurs; ambiguity stays Unknown until observed.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0165 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
