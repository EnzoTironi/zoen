# ZN-0166 — Continue one relationship across independently linked channels

**Status:** planned/unaccepted · **Spec:** [SPEC-028](../specs/spec-028.md) · **Milestone:** S5 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-028.md](../specs/spec-028.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-028](../algorithms/spec-028.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0165](zn-0165.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/adapters/src/channels/multichannel`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0166 /* planning label, not a public API */
  OWNER := SPEC-028; TARGET := packages/adapters/src/channels/multichannel/channel-continuity.ts
  REQUIRE accepted dependencies: ZN-0165
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Store authority-free Focus and settled interaction references at the relationship level.
    02. Require separate proof/consent for each channel binding.
    03. Recompile current authorized context after a channel/audience change.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN A user moves from WhatsApp to email then into a work group
    WHEN They reopen the same subject
    THEN Context continues only where permitted; the work group does not inherit private personal content
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/adapters/src/channels/multichannel/channel-continuity.ts` | [comment-only-source](../../packages/adapters/src/channels/multichannel/channel-continuity.ts) | Primary |
| `tests/component/spec-028/channel-continuity.test.ts` | [comment-only-source](../../tests/component/spec-028/channel-continuity.test.ts) | Required tests |
| `runbooks/spec-028/channel-continuity.md` | [markdown-plan](../../runbooks/spec-028/channel-continuity.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/adapters/src/channels/multichannel/channel-continuity.ts` — [plan](../../packages/adapters/src/channels/multichannel/channel-continuity.ts)
- `tests/component/spec-028/channel-continuity.test.ts` — [plan](../../tests/component/spec-028/channel-continuity.test.ts)
- `runbooks/spec-028/channel-continuity.md` — [plan](../../runbooks/spec-028/channel-continuity.md)
- `contracts/spec-028/channel-continuity.schema.json` — [plan](../../contracts/spec-028/channel-continuity.schema.json.plan.md)
- `tests/fixtures/spec-028/channel-continuity.json` — [plan](../../tests/fixtures/spec-028/channel-continuity.json.plan.md)
- `db/migrations/zn-0166_channel-continuity.sql` — [plan](../../db/migrations/zn-0166_channel-continuity.sql.plan.md)
- `tests/migrations/zn-0166.test.ts` — [plan](../../tests/migrations/zn-0166.test.ts)
- `packages/adapters/src/channels/multichannel/index.ts` — [plan](../../packages/adapters/src/channels/multichannel/index.ts)
- `packages/adapters/src/channels/multichannel/ports.ts` — [plan](../../packages/adapters/src/channels/multichannel/ports.ts)
- `packages/adapters/src/channels/multichannel/types.ts` — [plan](../../packages/adapters/src/channels/multichannel/types.ts)
- `apps/edge/src/composition.ts` — [plan](../../apps/edge/src/composition.ts)

## Required tests

```text
SUITE ZN-0166 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0166-AC:
    ARRANGE A user moves from WhatsApp to email then into a work group
    ACT They reopen the same subject
    ASSERT Context continues only where permitted; the work group does not inherit private personal content
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0166-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Treat email From, Telegram group presence or an old channel binding as proof of identity. No authority is inherited. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0166-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Interrupt after durable admission or provider acceptance but before acknowledgement. Retry with the same identity and an old worker fence. No duplicate committed result or unsafe resend occurs; ambiguity stays Unknown until observed.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0166 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
