# ZN-0199 — Define distributed and GPU execution lifecycle

**Status:** planned/unaccepted · **Spec:** [SPEC-034](../specs/spec-034.md) · **Milestone:** S7 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-034.md](../specs/spec-034.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-034](../algorithms/spec-034.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0198](zn-0198.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/ontology/src/compute`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0199 /* planning label, not a public API */
  OWNER := SPEC-034; TARGET := packages/ontology/src/compute/distributed-contract.ts
  REQUIRE accepted dependencies: ZN-0198
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Implement submit/running/cancelling/completed/failed/unknown states with stable job identity.
    02. Bind artifact, input versions, resource/budget constraints and output schema.
    03. Keep engine-specific implementation behind a port with an admission certificate.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN The engine accepts a compute job then loses the submission response
    WHEN The scheduler retries
    THEN It reconciles by stable job identity rather than spawning uncontrolled duplicate jobs; budget and unknown state remain explicit
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/compute/distributed-contract.ts` | [comment-only-source](../../packages/ontology/src/compute/distributed-contract.ts) | Primary |
| `tests/component/spec-034/distributed-contract.test.ts` | [comment-only-source](../../tests/component/spec-034/distributed-contract.test.ts) | Required tests |
| `runbooks/spec-034/distributed-contract.md` | [markdown-plan](../../runbooks/spec-034/distributed-contract.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/compute/distributed-contract.ts` — [plan](../../packages/ontology/src/compute/distributed-contract.ts)
- `tests/component/spec-034/distributed-contract.test.ts` — [plan](../../tests/component/spec-034/distributed-contract.test.ts)
- `runbooks/spec-034/distributed-contract.md` — [plan](../../runbooks/spec-034/distributed-contract.md)
- `contracts/spec-034/distributed-contract.schema.json` — [plan](../../contracts/spec-034/distributed-contract.schema.json.plan.md)
- `tests/fixtures/spec-034/distributed-contract.json` — [plan](../../tests/fixtures/spec-034/distributed-contract.json.plan.md)
- `db/migrations/zn-0199_distributed-contract.sql` — [plan](../../db/migrations/zn-0199_distributed-contract.sql.plan.md)
- `tests/migrations/zn-0199.test.ts` — [plan](../../tests/migrations/zn-0199.test.ts)
- `packages/ontology/src/compute/index.ts` — [plan](../../packages/ontology/src/compute/index.ts)
- `packages/ontology/src/compute/ports.ts` — [plan](../../packages/ontology/src/compute/ports.ts)
- `packages/ontology/src/compute/types.ts` — [plan](../../packages/ontology/src/compute/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-034.ts` — [plan](../../apps/authority-worker/src/registrations/spec-034.ts)

## Required tests

```text
SUITE ZN-0199 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0199-AC:
    ARRANGE The engine accepts a compute job then loses the submission response
    ACT The scheduler retries
    ASSERT It reconciles by stable job identity rather than spawning uncontrolled duplicate jobs; budget and unknown state remain explicit
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0199-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Exceed a query lease’s rights, budget, output or time limits. Cancel/bound execution and never publish partial output as complete. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0199-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Terminate execution after output staging or resource exhaustion. Leases expire, unadmitted output stays non-authoritative, and no resumed sandbox acquires broader authority.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0199 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
