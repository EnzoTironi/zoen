# ZN-0159 — Define ChartDocument and table/timeline contracts

**Status:** planned/unaccepted · **Spec:** [SPEC-027](../specs/spec-027.md) · **Milestone:** S5 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-027.md](../specs/spec-027.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [algorithm SPEC-027](../algorithms/spec-027.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0073](zn-0073.md), [ZN-0152](zn-0152.md), [ZN-0158](zn-0158.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:apps/web/src/living-world`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0159 /* planning label, not a public API */
  OWNER := SPEC-027; TARGET := apps/web/src/living-world/view-contracts.ts
  REQUIRE accepted dependencies: ZN-0073, ZN-0152, ZN-0158
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Declare axes, units, series, aggregation, null semantics and evidence links as data.
    02. Bind every view to operation/Frame schema and permitted renderers.
    03. Forbid arbitrary JavaScript formatters and source-provided executable HTML.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN A chart descriptor embeds a script and another mixes USD and BRL on one unlabeled series
    WHEN Validation runs
    THEN Both are rejected; valid charts retain explicit unit/currency and temporal basis
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `apps/web/src/living-world/view-contracts.ts` | [comment-only-source](../../apps/web/src/living-world/view-contracts.ts) | Primary |
| `tests/component/spec-027/view-contracts.test.ts` | [comment-only-source](../../tests/component/spec-027/view-contracts.test.ts) | Required tests |
| `runbooks/spec-027/view-contracts.md` | [markdown-plan](../../runbooks/spec-027/view-contracts.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `apps/web/src/living-world/view-contracts.ts` — [plan](../../apps/web/src/living-world/view-contracts.ts)
- `tests/component/spec-027/view-contracts.test.ts` — [plan](../../tests/component/spec-027/view-contracts.test.ts)
- `runbooks/spec-027/view-contracts.md` — [plan](../../runbooks/spec-027/view-contracts.md)
- `contracts/spec-027/view-contracts.schema.json` — [plan](../../contracts/spec-027/view-contracts.schema.json.plan.md)
- `tests/fixtures/spec-027/view-contracts.json` — [plan](../../tests/fixtures/spec-027/view-contracts.json.plan.md)
- `db/migrations/zn-0159_view-contracts.sql` — [plan](../../db/migrations/zn-0159_view-contracts.sql.plan.md)
- `tests/migrations/zn-0159.test.ts` — [plan](../../tests/migrations/zn-0159.test.ts)
- `apps/web/src/living-world/index.ts` — [plan](../../apps/web/src/living-world/index.ts)
- `apps/web/src/living-world/ports.ts` — [plan](../../apps/web/src/living-world/ports.ts)
- `apps/web/src/living-world/types.ts` — [plan](../../apps/web/src/living-world/types.ts)
- `apps/web/src/routes.tsx` — [plan](../../apps/web/src/routes.tsx)
- `apps/web/src/registrations/spec-027.tsx` — [plan](../../apps/web/src/registrations/spec-027.tsx)

## Required tests

```text
SUITE ZN-0159 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0159-AC:
    ARRANGE A chart descriptor embeds a script and another mixes USD and BRL on one unlabeled series
    ACT Validation runs
    ASSERT Both are rejected; valid charts retain explicit unit/currency and temporal basis
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0159-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Render a chart/table/export using a direct unauthorized index query. No data appears outside the pinned authorized Frame. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0159-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Repeat with duplicate/reordered input, revoked access and the profile limit at the task boundary. Preserve the declared oracle; report unsupported/incomplete state rather than silent truncation, disclosure or fabricated success.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0159 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
