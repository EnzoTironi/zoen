# ZN-0234 — Implement security audit exports and incident runbooks

**Status:** planned/unaccepted · **Spec:** [SPEC-040](../specs/spec-040.md) · **Milestone:** S9 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-040.md](../specs/spec-040.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-040](../algorithms/spec-040.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0233](zn-0233.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/ontology/src/operations`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0234 /* planning label, not a public API */
  OWNER := SPEC-040; TARGET := packages/ontology/src/operations/audit-export.ts
  REQUIRE accepted dependencies: ZN-0233
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Export permitted immutable receipt/security events to an admitted destination.
    02. Test redaction, tamper detection, retention and backpressure.
    03. Provide revoke/stop/isolate/restore playbooks with named roles and verification checks.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN An export destination fails and the incident involves a compromised runner
    WHEN Operators follow the runbook
    THEN Audit gaps are explicit, new runner permits stop and evidence is preserved without broad data disclosure
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/operations/audit-export.ts` | [comment-only-source](../../packages/ontology/src/operations/audit-export.ts) | Primary |
| `tests/component/spec-040/audit-export.test.ts` | [comment-only-source](../../tests/component/spec-040/audit-export.test.ts) | Required tests |
| `runbooks/spec-040/audit-export.md` | [markdown-plan](../../runbooks/spec-040/audit-export.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/operations/audit-export.ts` — [plan](../../packages/ontology/src/operations/audit-export.ts)
- `tests/component/spec-040/audit-export.test.ts` — [plan](../../tests/component/spec-040/audit-export.test.ts)
- `runbooks/spec-040/audit-export.md` — [plan](../../runbooks/spec-040/audit-export.md)
- `contracts/spec-040/audit-export.schema.json` — [plan](../../contracts/spec-040/audit-export.schema.json.plan.md)
- `tests/fixtures/spec-040/audit-export.json` — [plan](../../tests/fixtures/spec-040/audit-export.json.plan.md)
- `db/migrations/zn-0234_audit-export.sql` — [plan](../../db/migrations/zn-0234_audit-export.sql.plan.md)
- `tests/migrations/zn-0234.test.ts` — [plan](../../tests/migrations/zn-0234.test.ts)
- `packages/ontology/src/operations/index.ts` — [plan](../../packages/ontology/src/operations/index.ts)
- `packages/ontology/src/operations/ports.ts` — [plan](../../packages/ontology/src/operations/ports.ts)
- `packages/ontology/src/operations/types.ts` — [plan](../../packages/ontology/src/operations/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-040.ts` — [plan](../../apps/authority-worker/src/registrations/spec-040.ts)

## Required tests

```text
SUITE ZN-0234 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0234-AC:
    ARRANGE An export destination fails and the incident involves a compromised runner
    ACT Operators follow the runbook
    ASSERT Audit gaps are explicit, new runner permits stop and evidence is preserved without broad data disclosure
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0234-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT A tenant exceeds quota or support requests content without authority. Bound resource use and require audited narrow access. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0234-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Repeat with duplicate/reordered input, revoked access and the profile limit at the task boundary. Preserve the declared oracle; report unsupported/incomplete state rather than silent truncation, disclosure or fabricated success.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0234 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
