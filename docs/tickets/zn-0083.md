# ZN-0083 — Provision an isolated EvaluationWorld

**Status:** planned/unaccepted · **Spec:** [SPEC-014](../specs/spec-014.md) · **Milestone:** S2 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-014.md](../specs/spec-014.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [algorithm SPEC-014](../algorithms/spec-014.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0082](zn-0082.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/ontology/src/releases`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0083 /* planning label, not a public API */
  OWNER := SPEC-014; TARGET := packages/ontology/src/releases/evaluation-world.ts
  REQUIRE accepted dependencies: ZN-0082
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Allocate distinct realm IDs, database roles, object namespace and channel sinks.
    02. Deny production secret/endpoint access independently of application logic.
    03. Run normal semantic surfaces against the candidate release inside evaluation.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN A candidate connector tries to use a live credential or address a live WhatsApp recipient
    WHEN The evaluation journey executes
    THEN Infrastructure and lease checks deny both; the proof records missing external attestations instead of fabricated success
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/releases/evaluation-world.ts` | [comment-only-source](../../packages/ontology/src/releases/evaluation-world.ts) | Primary |
| `tests/component/spec-014/evaluation-world.test.ts` | [comment-only-source](../../tests/component/spec-014/evaluation-world.test.ts) | Required tests |
| `runbooks/spec-014/evaluation-world.md` | [markdown-plan](../../runbooks/spec-014/evaluation-world.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/releases/evaluation-world.ts` — [plan](../../packages/ontology/src/releases/evaluation-world.ts)
- `tests/component/spec-014/evaluation-world.test.ts` — [plan](../../tests/component/spec-014/evaluation-world.test.ts)
- `runbooks/spec-014/evaluation-world.md` — [plan](../../runbooks/spec-014/evaluation-world.md)
- `contracts/spec-014/evaluation-world.schema.json` — [plan](../../contracts/spec-014/evaluation-world.schema.json.plan.md)
- `tests/fixtures/spec-014/evaluation-world.json` — [plan](../../tests/fixtures/spec-014/evaluation-world.json.plan.md)
- `db/migrations/zn-0083_evaluation-world.sql` — [plan](../../db/migrations/zn-0083_evaluation-world.sql.plan.md)
- `tests/migrations/zn-0083.test.ts` — [plan](../../tests/migrations/zn-0083.test.ts)
- `packages/ontology/src/releases/index.ts` — [plan](../../packages/ontology/src/releases/index.ts)
- `packages/ontology/src/releases/ports.ts` — [plan](../../packages/ontology/src/releases/ports.ts)
- `packages/ontology/src/releases/types.ts` — [plan](../../packages/ontology/src/releases/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-014.ts` — [plan](../../apps/authority-worker/src/registrations/spec-014.ts)

## Required tests

```text
SUITE ZN-0083 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0083-AC:
    ARRANGE A candidate connector tries to use a live credential or address a live WhatsApp recipient
    ACT The evaluation journey executes
    ASSERT Infrastructure and lease checks deny both; the proof records missing external attestations instead of fabricated success
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0083-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Let the candidate policy approve itself or address a live destination from evaluation. Both are denied under current authority. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0083-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT For this operation, race duplicate delivery/replay and a relevant dependency change at a named commit boundary. At most one same-intent semantic result commits; stale work is rejected. For read-only/validation work, prove deterministic output at the same basis and explicit stale/unsupported output at the changed basis.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-08: barrier=after preparation, before head lock; inject=Concurrent evidence and policy change; assert=PreparationStale; no mixed generation.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0083 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
