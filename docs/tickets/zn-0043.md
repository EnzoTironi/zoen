# ZN-0043 — Implement semantic dispatcher and stable error translation

**Status:** planned/unaccepted · **Spec:** [SPEC-007](../specs/spec-007.md) · **Milestone:** S0 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-007.md](../specs/spec-007.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [algorithm SPEC-007](../algorithms/spec-007.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0042](zn-0042.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `contract:semantic-executor`, `module:packages/ontology/src/surfaces`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0043 /* planning label, not a public API */
  OWNER := SPEC-007; TARGET := packages/ontology/src/surfaces/dispatch.ts
  REQUIRE accepted dependencies: ZN-0042
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Dispatch only known operation IDs with schema and contract digest validation.
    02. Map domain errors through the common disclosure-safe envelope.
    03. Prevent direct table names, arbitrary SQL and unregistered invoke methods at every entrypoint.
    04. V4: use the existing SPEC-007 dispatcher and shared SemanticClient; validate no per-surface authorization or reconciliation implementation is introduced.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN A valid inspect request and requests containing SQL, unknown operation and mismatched digest
    WHEN They reach web and CLI adapters
    THEN The valid request executes once; the others fail explicitly with no raw database access or silent version fallback
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/surfaces/dispatch.ts` | [existing-with-sidecar](../../packages/ontology/src/surfaces/dispatch.ts.plan.md) | Primary |
| `tests/component/spec-007/dispatch.test.ts` | [comment-only-source](../../tests/component/spec-007/dispatch.test.ts) | Required tests |
| `runbooks/spec-007/dispatch.md` | [markdown-plan](../../runbooks/spec-007/dispatch.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/surfaces/dispatch.ts` — [plan](../../packages/ontology/src/surfaces/dispatch.ts.plan.md)
- `tests/component/spec-007/dispatch.test.ts` — [plan](../../tests/component/spec-007/dispatch.test.ts)
- `runbooks/spec-007/dispatch.md` — [plan](../../runbooks/spec-007/dispatch.md)
- `contracts/spec-007/dispatch.schema.json` — [plan](../../contracts/spec-007/dispatch.schema.json.plan.md)
- `tests/fixtures/spec-007/dispatch.json` — [plan](../../tests/fixtures/spec-007/dispatch.json.plan.md)
- `db/migrations/zn-0043_dispatch.sql` — [plan](../../db/migrations/zn-0043_dispatch.sql.plan.md)
- `tests/migrations/zn-0043.test.ts` — [plan](../../tests/migrations/zn-0043.test.ts)
- `packages/ontology/src/surfaces/index.ts` — [plan](../../packages/ontology/src/surfaces/index.ts)
- `packages/ontology/src/surfaces/ports.ts` — [plan](../../packages/ontology/src/surfaces/ports.ts)
- `packages/ontology/src/surfaces/types.ts` — [plan](../../packages/ontology/src/surfaces/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-007.ts` — [plan](../../apps/authority-worker/src/registrations/spec-007.ts)

## Required tests

```text
SUITE ZN-0043 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0043-AC:
    ARRANGE A valid inspect request and requests containing SQL, unknown operation and mismatched digest
    ACT They reach web and CLI adapters
    ASSERT The valid request executes once; the others fail explicitly with no raw database access or silent version fallback
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0043-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Read an opaque ref without current permission. No data, row count, existence metadata or evidence URL leaks. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0043-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Repeat with duplicate/reordered input, revoked access and the profile limit at the task boundary. Preserve the declared oracle; report unsupported/incomplete state rather than silent truncation, disclosure or fabricated success.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0043 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
