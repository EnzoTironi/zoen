# ZN-0251 — Build independent-cut federated Frames

**Status:** planned/unaccepted · **Spec:** [SPEC-043](../specs/spec-043.md) · **Milestone:** S10 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-043.md](../specs/spec-043.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-043](../algorithms/spec-043.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0250](zn-0250.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/ontology/src/federation`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0251 /* planning label, not a public API */
  OWNER := SPEC-043; TARGET := packages/ontology/src/federation/federated-read.ts
  REQUIRE accepted dependencies: ZN-0250
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Collect exact component Frames and their release/cut/rights/freshness.
    02. Label skew, missing components and non-comparable semantics.
    03. Reauthorize redisclosure and avoid globally instantaneous claims.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN Two regions return data from different cuts and one participant is unavailable
    WHEN An aggregate view is composed
    THEN The output shows independent bases and incomplete coverage; it never claims a complete atomic global total
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/federation/federated-read.ts` | [comment-only-source](../../packages/ontology/src/federation/federated-read.ts) | Primary |
| `tests/component/spec-043/federated-read.test.ts` | [comment-only-source](../../tests/component/spec-043/federated-read.test.ts) | Required tests |
| `runbooks/spec-043/federated-read.md` | [markdown-plan](../../runbooks/spec-043/federated-read.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/federation/federated-read.ts` — [plan](../../packages/ontology/src/federation/federated-read.ts)
- `tests/component/spec-043/federated-read.test.ts` — [plan](../../tests/component/spec-043/federated-read.test.ts)
- `runbooks/spec-043/federated-read.md` — [plan](../../runbooks/spec-043/federated-read.md)
- `contracts/spec-043/federated-read.schema.json` — [plan](../../contracts/spec-043/federated-read.schema.json.plan.md)
- `tests/fixtures/spec-043/federated-read.json` — [plan](../../tests/fixtures/spec-043/federated-read.json.plan.md)
- `db/migrations/zn-0251_federated-read.sql` — [plan](../../db/migrations/zn-0251_federated-read.sql.plan.md)
- `tests/migrations/zn-0251.test.ts` — [plan](../../tests/migrations/zn-0251.test.ts)
- `packages/ontology/src/federation/index.ts` — [plan](../../packages/ontology/src/federation/index.ts)
- `packages/ontology/src/federation/ports.ts` — [plan](../../packages/ontology/src/federation/ports.ts)
- `packages/ontology/src/federation/types.ts` — [plan](../../packages/ontology/src/federation/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-043.ts` — [plan](../../apps/authority-worker/src/registrations/spec-043.ts)

## Required tests

```text
SUITE ZN-0251 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0251-AC:
    ARRANGE Two regions return data from different cuts and one participant is unavailable
    ACT An aggregate view is composed
    ASSERT The output shows independent bases and incomplete coverage; it never claims a complete atomic global total
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0251-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Treat remote approval or a timestamp as local authority/coherent global cut. Reject the implied authority and preserve independent bases. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0251-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Inject source/process/network loss during rollout or restore. Reject unsafe writes until required fencing and recovery evidence exists; preserve explicit partial or unknown external outcomes.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-18: barrier=one local commit, other rejects; inject=Partial remote failure; assert=Independent receipts, explicit partial compensation options.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0251 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
