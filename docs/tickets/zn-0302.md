# ZN-0302 — Define bounded MiniAppDefinition and manifest schemas

**Status:** planned/unaccepted · **Spec:** [SPEC-052](../specs/spec-052.md) · **Milestone:** S2 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-052.md](../specs/spec-052.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [docs/architecture/mini-app-contract.md](../architecture/mini-app-contract.md)
- [algorithm SPEC-052](../algorithms/spec-052.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0078](zn-0078.md), [ZN-0291](zn-0291.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/ontology/src/apps`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0302 /* planning label, not a public API */
  OWNER := SPEC-052; TARGET := packages/ontology/src/apps/app-definition.ts
  REQUIRE accepted dependencies: ZN-0078, ZN-0291
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Add declarative routes, components, form bindings, read/action references and declared capability ceiling to the released grammar.
    02. Validate stable IDs, dependency closure, labels, bounded nodes and per-view query budgets.
    03. Refuse executable expressions and external fetch URLs; use a template instance lane only when the existing policy explicitly covers exact parameters.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN An admitted order type, production view and existing Inspect operation
    WHEN A model proposes a board with table/detail/filter widgets and released bindings
    THEN A deterministic validated manifest is emitted without adding TypeScript domain code or a new backend
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/ontology/src/apps/app-definition.ts` | [comment-only-source](../../packages/ontology/src/apps/app-definition.ts) | Primary |
| `tests/component/spec-052/app-definition.test.ts` | [comment-only-source](../../tests/component/spec-052/app-definition.test.ts) | Required tests |
| `runbooks/spec-052/app-definition.md` | [markdown-plan](../../runbooks/spec-052/app-definition.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/ontology/src/apps/app-definition.ts` — [plan](../../packages/ontology/src/apps/app-definition.ts)
- `tests/component/spec-052/app-definition.test.ts` — [plan](../../tests/component/spec-052/app-definition.test.ts)
- `runbooks/spec-052/app-definition.md` — [plan](../../runbooks/spec-052/app-definition.md)
- `contracts/spec-052/app-definition.schema.json` — [plan](../../contracts/spec-052/app-definition.schema.json.plan.md)
- `tests/fixtures/spec-052/app-definition.json` — [plan](../../tests/fixtures/spec-052/app-definition.json.plan.md)
- `db/migrations/zn-0302_app-definition.sql` — [plan](../../db/migrations/zn-0302_app-definition.sql.plan.md)
- `tests/migrations/zn-0302.test.ts` — [plan](../../tests/migrations/zn-0302.test.ts)
- `packages/ontology/src/apps/index.ts` — [plan](../../packages/ontology/src/apps/index.ts)
- `packages/ontology/src/apps/ports.ts` — [plan](../../packages/ontology/src/apps/ports.ts)
- `packages/ontology/src/apps/types.ts` — [plan](../../packages/ontology/src/apps/types.ts)
- `apps/authority-worker/src/composition.ts` — [plan](../../apps/authority-worker/src/composition.ts)
- `apps/authority-worker/src/registrations/spec-052.ts` — [plan](../../apps/authority-worker/src/registrations/spec-052.ts)
- `packages/contracts/src/mini-app.ts` — [plan](../../packages/contracts/src/mini-app.ts)
- `contracts/mini-app-definition.schema.json` — [plan](../../contracts/mini-app-definition.schema.json.plan.md)

## Required tests

```text
SUITE ZN-0302 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0302-AC:
    ARRANGE An admitted order type, production view and existing Inspect operation
    ACT A model proposes a board with table/detail/filter widgets and released bindings
    ASSERT A deterministic validated manifest is emitted without adding TypeScript domain code or a new backend
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0302-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Unknown widget, script tag, SQL, principal field, prototype-polluting key or unbounded query rejects the draft
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0302-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Reorder equivalent maps and recompile; digest stays equal; exceed component/query limits and validation fails explicitly
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0302 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
