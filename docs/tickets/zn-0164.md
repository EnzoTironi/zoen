# ZN-0164 — Implement Telegram ingress and delivery adapter

**Status:** planned/unaccepted · **Spec:** [SPEC-028](../specs/spec-028.md) · **Milestone:** S5 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-028.md](../specs/spec-028.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [algorithm SPEC-028](../algorithms/spec-028.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0068](zn-0068.md), [ZN-0111](zn-0111.md), [ZN-0158](zn-0158.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `module:packages/adapters/src/channels/multichannel`, `schema:authority`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0164 /* planning label, not a public API */
  OWNER := SPEC-028; TARGET := packages/adapters/src/channels/multichannel/telegram.ts
  REQUIRE accepted dependencies: ZN-0068, ZN-0111, ZN-0158
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Admit documented webhook secret verification and owned bot/account configuration.
    02. Use stable update/chat/message namespaces and durable admission-before-ACK.
    03. Track send observations without inventing delivery/read evidence.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN A duplicate update and one with an invalid webhook secret arrive
    WHEN The adapter processes them
    THEN The valid update creates one turn; the invalid update is rejected and cannot impersonate a bound principal
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/adapters/src/channels/multichannel/telegram.ts` | [comment-only-source](../../packages/adapters/src/channels/multichannel/telegram.ts) | Primary |
| `tests/component/spec-028/telegram.test.ts` | [comment-only-source](../../tests/component/spec-028/telegram.test.ts) | Required tests |
| `runbooks/spec-028/telegram.md` | [markdown-plan](../../runbooks/spec-028/telegram.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/adapters/src/channels/multichannel/telegram.ts` — [plan](../../packages/adapters/src/channels/multichannel/telegram.ts)
- `tests/component/spec-028/telegram.test.ts` — [plan](../../tests/component/spec-028/telegram.test.ts)
- `runbooks/spec-028/telegram.md` — [plan](../../runbooks/spec-028/telegram.md)
- `contracts/spec-028/telegram.schema.json` — [plan](../../contracts/spec-028/telegram.schema.json.plan.md)
- `tests/fixtures/spec-028/telegram.json` — [plan](../../tests/fixtures/spec-028/telegram.json.plan.md)
- `db/migrations/zn-0164_telegram.sql` — [plan](../../db/migrations/zn-0164_telegram.sql.plan.md)
- `tests/migrations/zn-0164.test.ts` — [plan](../../tests/migrations/zn-0164.test.ts)
- `packages/adapters/src/channels/multichannel/index.ts` — [plan](../../packages/adapters/src/channels/multichannel/index.ts)
- `packages/adapters/src/channels/multichannel/ports.ts` — [plan](../../packages/adapters/src/channels/multichannel/ports.ts)
- `packages/adapters/src/channels/multichannel/types.ts` — [plan](../../packages/adapters/src/channels/multichannel/types.ts)
- `apps/edge/src/composition.ts` — [plan](../../apps/edge/src/composition.ts)

## Required tests

```text
SUITE ZN-0164 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0164-AC:
    ARRANGE A duplicate update and one with an invalid webhook secret arrive
    ACT The adapter processes them
    ASSERT The valid update creates one turn; the invalid update is rejected and cannot impersonate a bound principal
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0164-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT Treat email From, Telegram group presence or an old channel binding as proof of identity. No authority is inherited. Exercise this against the component delivered by this ticket; do not require a later-stage feature to implement an early negative check.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0164-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Interrupt after durable admission or provider acceptance but before acknowledgement. Retry with the same identity and an old worker fence. No duplicate committed result or unsafe resend occurs; ambiguity stays Unknown until observed.
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0164 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
