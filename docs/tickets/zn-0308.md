# ZN-0308 — Provision isolated origins and signed private asset serving

**Status:** planned/unaccepted · **Spec:** [SPEC-053](../specs/spec-053.md) · **Milestone:** S6 · **Layer:** component · **Risk:** critical

## Read set

- [docs/specs/spec-053.md](../specs/spec-053.md)
- [docs/architecture/constitution.md](../architecture/constitution.md)
- [docs/architecture/repository-contract.md](../architecture/repository-contract.md)
- [docs/architecture/protocol-contract.md](../architecture/protocol-contract.md)
- [docs/testing/strategy.md](../testing/strategy.md)
- [docs/architecture/semantic-path.md](../architecture/semantic-path.md)
- [docs/architecture/app-host-security.md](../architecture/app-host-security.md)
- [algorithm SPEC-053](../algorithms/spec-053.md)

## Prerequisites and stop conditions

Accepted dependencies: [ZN-0171](zn-0171.md), [ZN-0174](zn-0174.md), [ZN-0299](zn-0299.md).
External gate: `none on this ticket`. Resource locks: `composition:trusted-roots`, `infra:app-host`, `module:packages/clients/src/app-host`.

Do not infer acceptance from inherited candidate code. Missing actual services or unresolved schemas block the applicable check. No mock provider, offline success branch or broadened permission is allowed.

## Bounded pseudocode

```text
PROCEDURE ZN_0308 /* planning label, not a public API */
  OWNER := SPEC-053; TARGET := packages/clients/src/app-host/app-origins.ts
  REQUIRE accepted dependencies: ZN-0171, ZN-0174, ZN-0299
  REQUIRE evidence layer: component; actual admitted services when needed
  IF a required service/profile/schema is missing: STOP Blocked; never substitute a provider.
  IF normative contracts conflict: STOP SpecConflict; never choose a permissive interpretation.
  TICKET-SPECIFIC SEGMENT:
    01. Separate trusted host and guest on distinct registered-site boundaries under an admitted TLS/DNS profile.
    02. Serve digest-addressed audited bundles only after host authorization; no data or secrets inside bundle.
    03. Enforce CSP, sandbox flags, no shared cookies, no-store private assets fetched by the trusted host and transferred to a generic guest loader over the bound MessageChannel; no third-party cookies or URL grants; deny controlled egress APIs/top navigation/forms/workers and require the explicit executable data-disclosure profile.
  TEST BEFORE DECLARING THIS SEGMENT COMPLETE:
    GIVEN Two users with different rights load the same signed generated app
    WHEN The browser requests HTML, scripts, assets, icons and source maps directly and through the host
    THEN Only the authorized bundle is served; no source map, manifest, private title or data appears through an unauthenticated route
  ON failure: preserve observed state and evidence; no fabricated success or consent refresh.
  RETURN only the owning spec's tagged result / recorded test evidence for the exact ticket.
```

## Exact file map

| Artifact | Plan / existing file | Role |
|---|---|---|
| `packages/clients/src/app-host/app-origins.ts` | [comment-only-source](../../packages/clients/src/app-host/app-origins.ts) | Primary |
| `tests/component/spec-053/app-origins.test.ts` | [comment-only-source](../../tests/component/spec-053/app-origins.test.ts) | Required tests |
| `runbooks/spec-053/app-origins.md` | [markdown-plan](../../runbooks/spec-053/app-origins.md) | Repair runbook |

## Write allowlist

Conditional support paths are reserved, not a command to create unused schemas/migrations. A `.plan.md` is not the corresponding executable JSON/SQL/config. Promote only required artifacts under this ticket and review.

- `packages/clients/src/app-host/app-origins.ts` — [plan](../../packages/clients/src/app-host/app-origins.ts)
- `tests/component/spec-053/app-origins.test.ts` — [plan](../../tests/component/spec-053/app-origins.test.ts)
- `runbooks/spec-053/app-origins.md` — [plan](../../runbooks/spec-053/app-origins.md)
- `contracts/spec-053/app-origins.schema.json` — [plan](../../contracts/spec-053/app-origins.schema.json.plan.md)
- `tests/fixtures/spec-053/app-origins.json` — [plan](../../tests/fixtures/spec-053/app-origins.json.plan.md)
- `packages/clients/src/app-host/index.ts` — [plan](../../packages/clients/src/app-host/index.ts)
- `packages/clients/src/app-host/ports.ts` — [plan](../../packages/clients/src/app-host/ports.ts)
- `packages/clients/src/app-host/types.ts` — [plan](../../packages/clients/src/app-host/types.ts)
- `apps/edge/src/composition.ts` — [plan](../../apps/edge/src/composition.ts)
- `apps/edge/src/routes/app-assets.ts` — [plan](../../apps/edge/src/routes/app-assets.ts)
- `infra/terraform/app-host/main.tf` — [plan](../../infra/terraform/app-host/main.tf.plan.md)
- `infra/terraform/app-host/variables.tf` — [plan](../../infra/terraform/app-host/variables.tf.plan.md)

## Required tests

```text
SUITE ZN-0308 [required layer=component; currently NOT IMPLEMENTED]
  REQUIRE real admitted dependencies and disposable owned namespaces; missing dependency => BLOCKED.
  USE synthetic input records, not synthetic services, fake credentials or canned provider responses.

  TEST ZN-0308-AC:
    ARRANGE Two users with different rights load the same signed generated app
    ACT The browser requests HTML, scripts, assets, icons and source maps directly and through the host
    ASSERT Only the authorized bundle is served; no source map, manifest, private title or data appears through an unauthenticated route
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0308-NEG:
    ARRANGE the same ticket component with the stated invalid/denied input.
    ACT only through its real supported boundary; inspect denial and lack of side effects.
    ASSERT A guest iframe cannot read host cookies, storage or parent DOM and cannot register a service worker controlling the trusted host
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  TEST ZN-0308-BOUNDARY:
    ARRANGE the same component at its named failure/replay/resource boundary.
    ACT with independently controlled real connection/process barriers when I/O is involved.
    ASSERT Domain/asset cache changes fail admission until the exact profile is rechecked; executable code is not enabled by a DNS name alone
    CAPTURE commit, actual profile/lock, fixture seed, executed count and raw observations.

  AVAILABLE SPEC-LOCAL FAULT BOUNDARIES (apply only when owned by this ticket):
    F-27: barrier=after iframe navigation; inject=Replay old source window/channel messages; assert=No dispatch under replacement session.
    F-28: barrier=before warm instance reuse; inject=Alternate owner/worker and restore snapshot; assert=No cross-subject/purpose private state or stale grant.
    F-34: barrier=before supplying protected Frame; inject=Remove executable disclosure admission; assert=No protected guest payload; trusted renderer/fail-closed.
    F-35: barrier=during resource/tool negotiation; inject=Host omits required security capability; assert=Text/protected-link fallback without authority leak.

  ASSERT every required check executed, no required skips, nonzero count and exact semantic oracle.
  CLEANUP only this test namespace after checking receipts/pins/unknown external outcomes.
  NEVER expose clocks, barriers, fixture seeders or failure controls in production routes.
```

## Verification and acceptance

```sh
pnpm verify:ticket --ticket ZN-0308 --profile <admitted-component-profile>
```

This target verification command is itself delivered by SPEC-000; it is not silently mapped to the existing 72 core tests. Preserve all three required check IDs. Report exact commit, genuine lock, fixture/version/seed, actual dependency profile, raw observations, no skipped checks, failure reproduction and independent review.

## Failure and repair

Preserve committed state. Retain unknown external outcomes; retry only under the owning idempotency contract. Keep new capabilities disabled until qualified. Schema repair is a reviewed forward transition where rollback would lose data. Do not mark this ticket accepted until the existing evidence validator verifies its evidence and prerequisites.
