# Anti-list — preserve ambition, do not manufacture current work

Atlas / architecture images are **non-normative target views**. Principles (one domain path, knowledge ownership) remain useful. They are **not** a bill of materials. No image overrides current source or product policy.

Condensed from the post-#74 audit `ANTI-LIST.md`.

| Item | Observed / intended | Not now |
| --- | --- | --- |
| **GraphQL** | Not in scanned runtime; not required by an active seam | No GraphQL ticket or dependency |
| **Temporal workflow / cloud** | Infographics conflated clock/time with a workflow platform | No Temporal/Restate reintroduction; `TemporalBasis` ≠ Temporal engine |
| **Effect Workflow / Cluster as current service** | Atlas target only; no reachable composition established | No infra ticket just to match the drawing |
| **Cedar** | Not a scanned dependency; rights live in existing access/SQL paths | Do not describe current auth as Cedar |
| **SSE / production streaming** | No text/event-stream route found in scoped scan | Do not label buffered JSON as qualified streaming |
| **Kubernetes / HA / extra Fly staging** | Not current ops; paid staging rejected | No k8s manifests, `infra/` tree, HA claim, or staging Fly app |
| **Door / client / ui / global adapters packages** | Concept labels, not current ownership needs | No wrapper-only packages or empty exports |
| **worker / app-host / runner** | Unbuilt host boundaries | No jobs/containers by atlas implication |
| **packs/ and extensions/** | Four ICPs ≠ installable runtime packs | No fake persona packs, skeletons, or customer branches |
| **Mini-app Studio / Rivet / MCP Apps** | Full-horizon ambition retained | No Rivet deployment or plugin registry in this frontier |
| **Enterprise cells / federation / offline / market execution** | Long-horizon scope | No cell directories or financial effects now |
| **Multi-provider / cloud speech** | Full D05 beyond minimum one-provider text | Do not stall core/naming work on missing cloud keys |
| **Next.js / Hono / generic platform utils** | Decorative examples ≠ React/Vite + Effect HttpApi | No framework replacement to match an image |
| **Canonicalize / Ajv / decimal.js / package rewrite** | Older plan choices do not auto-override current libs | No blanket dependency rewrite in structural work |
| **Legacy destruction / DNS / Dependabot / #74 review queue** | Outside this audit | Do not destroy app `zoen` or replay operational commands from the ZIP |

**Trigger rule:** only open a separate slice when a real capability needs the boundary — never to make a diagram true.
