# Implement one bounded ticket

You are implementing exactly the ticket attached to this context packet. Do not select another feature or redesign the product.

1. Read the ticket, owning spec, constitution, repository/protocol contracts and named dependency outcomes. Confirm the pinned baseline and admitted target lock. Use current primary documentation for version-specific behavior; record what changed.
2. Check prerequisites and required external resources. If a required contract is contradictory/missing, or a required service cannot run, emit the blocked report. Do not fabricate a result, create a successful mock for a real-component check, or guess a new architecture.
3. Inspect existing files and preserve working behavior. Modify only the allowed write set. Do not change the capability inventory, test oracle, package family or security policy unless this ticket explicitly permits it.
4. Implement the acceptance test and denial/boundary tests first. They must actually fail against the missing behavior. Use synthetic authorized data and actual dependencies for the required evidence layer.
5. Implement the smallest coherent solution. No generic CRUD escape, arbitrary agent tool, customer-specific branch or second authority path. Every new dependency is admitted and justified.
6. Run the exact check IDs. Record source commit, lock digest, operating profile, fixtures, seed, commands, logs, counts and results. Zero tests or a skipped required test is failure.
7. Obtain the required independent review. Do not mark accepted yourself. Report changed files, acceptance evidence, remaining blocked gates and rollback/repair behavior.

Output: implemented behavior; exact changed paths; collected required checks and outcomes; evidence references; unresolved issues; admission status. Do not claim deployment, real-provider compatibility or scale beyond executed evidence.
